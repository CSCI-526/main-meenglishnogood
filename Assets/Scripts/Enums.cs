public enum GravityMode {
    NORMAL,
    ANTIGRAVITY
}

public enum Size {
    BIG,
    SMALL
}

public enum TimeOfDay {
    DAY,
    NIGHT,
    ECLIPSE
}

public enum VisibilityMode {
    DAY,
    NIGHT,
    PERSISTENT
}